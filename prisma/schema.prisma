generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario{
  usu_id    Int   @id @default(autoincrement())
  usu_nome  String
  usu_senha String
}
