generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Usuario{
  usu_id    Int   @id @default(autoincrement())
  usu_nome  String
  usu_senha String
}

// Usuario

//  - Criar campo de email

// PagamentoPix

//  - Criar a tabela e campos:
//     - id, nome, valortotal, datadecriacao, numerodeparticipantes, chavepix
//  - datadecricao tem um valor default do prisma que salva a
//      data de quando o registro foi criado, só pesquisar no site ou perguntar pro chat
//  - Criar a Controller, Service E Reporitory com incluir, alterar, excluir.
